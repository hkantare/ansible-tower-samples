- name: Hello World Sample
  hosts: localhost
  environment:
    PATH: "/usr/bin:{{ ansible_env.PATH }}"
  tasks:
    - name: Hello Message
      debug:
        var: ansible_facts
    - name: Create SSH
      openssh_keypair:
        path: '/tmp/id_ssh_rsa'
      register: ssh_key_local
    
